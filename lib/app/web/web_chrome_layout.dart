/// Shared layout metrics for web shell chrome and the request workspace bar.

/// Height for single-line chrome inputs (collections explorer search, request URL
/// bar) so they align visually. Matches explorer search prefix/suffix min height.
const double kWebChromeSingleLineFieldHeight = 40;
